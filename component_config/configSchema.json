{
  "type": "object",
  "title": "Application configuration",
  "required": [
    "#api_token",
    "endpoint",
    "sync_id"
  ],
  "properties": {
    "#api_token": {
      "type": "string",
      "format": "password",
      "title": "Enter API Key"
    },
    "endpoint": {
      "type": "array",
      "maxItems": 1,
      "title": "Select endpoint",
      "items": {
        "enum": [
        "Run Sync"
      ]
    },
    "sync_id": {
      "type": "string",
      "title": "Enter Sync ID"
    }
  }
  }
}
